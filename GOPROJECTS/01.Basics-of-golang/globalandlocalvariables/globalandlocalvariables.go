package main

//local variable are use inside the function (or) with in the programing block
//global variables are used outside of the function
import "fmt"

var j int = 50

func main() {
	var i int = 80
	fmt.Println(i)
	fmt.Println(j)
}
